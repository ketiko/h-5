<%= react_component('AppreciationForm', {
  action: "#{root_path}appreciations/#{params[:id]}",
  appreciation: @appreciation,
  errors: @appreciation.errors,
  csrf_token: form_authenticity_token,
  edit: true,
}) %>

<%= link_to 'Show', @appreciation %> |
<%= link_to 'Back', appreciations_path %>
